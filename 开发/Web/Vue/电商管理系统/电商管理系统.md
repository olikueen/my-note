# 1 项目概述

功能模块划分

```
用户登录
退出登录
用户管理
权限管理(角色权限 权限列表)
商品管理(商品列表 分类管理 参数管理)
订单管理
数据统计
```

# 2 项目初始化

```shell
# 1. 安装Vue脚手架
yarn global add --registry https://registry.npm.taobao.org @vue/cli
# 2. 通过Vue脚手架创建项目, 配置Vue路由
vue ui 

打开图像界面 -> 创建(创建项目) -> 手动(Babel Router Linter/Formatter 使用配置文件) -> Stander Config -> 创建项目

# 3. 配置 Element-UI 组件库
插件  添加插件 -> vue-cli-plugin-element -> import on demand

# 5. 配置axios库
依赖 -> 安装依赖 -> axios

# 6. 初始化Git远程仓库

# 7. 本地项目托管GitHub

```

```
npm install --registry https://registry.npm.taobao.org -g node-gyp
```

